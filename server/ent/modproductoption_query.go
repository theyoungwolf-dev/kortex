// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Dan6erbond/revline/ent/media"
	"github.com/Dan6erbond/revline/ent/mod"
	"github.com/Dan6erbond/revline/ent/modproductoption"
	"github.com/Dan6erbond/revline/ent/predicate"
	"github.com/google/uuid"
)

// ModProductOptionQuery is the builder for querying ModProductOption entities.
type ModProductOptionQuery struct {
	config
	ctx            *QueryContext
	order          []modproductoption.OrderOption
	inters         []Interceptor
	predicates     []predicate.ModProductOption
	withMod        *ModQuery
	withMedia      *MediaQuery
	withFKs        bool
	modifiers      []func(*sql.Selector)
	loadTotal      []func(context.Context, []*ModProductOption) error
	withNamedMedia map[string]*MediaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ModProductOptionQuery builder.
func (mpoq *ModProductOptionQuery) Where(ps ...predicate.ModProductOption) *ModProductOptionQuery {
	mpoq.predicates = append(mpoq.predicates, ps...)
	return mpoq
}

// Limit the number of records to be returned by this query.
func (mpoq *ModProductOptionQuery) Limit(limit int) *ModProductOptionQuery {
	mpoq.ctx.Limit = &limit
	return mpoq
}

// Offset to start from.
func (mpoq *ModProductOptionQuery) Offset(offset int) *ModProductOptionQuery {
	mpoq.ctx.Offset = &offset
	return mpoq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mpoq *ModProductOptionQuery) Unique(unique bool) *ModProductOptionQuery {
	mpoq.ctx.Unique = &unique
	return mpoq
}

// Order specifies how the records should be ordered.
func (mpoq *ModProductOptionQuery) Order(o ...modproductoption.OrderOption) *ModProductOptionQuery {
	mpoq.order = append(mpoq.order, o...)
	return mpoq
}

// QueryMod chains the current query on the "mod" edge.
func (mpoq *ModProductOptionQuery) QueryMod() *ModQuery {
	query := (&ModClient{config: mpoq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mpoq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mpoq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(modproductoption.Table, modproductoption.FieldID, selector),
			sqlgraph.To(mod.Table, mod.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, modproductoption.ModTable, modproductoption.ModColumn),
		)
		fromU = sqlgraph.SetNeighbors(mpoq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMedia chains the current query on the "media" edge.
func (mpoq *ModProductOptionQuery) QueryMedia() *MediaQuery {
	query := (&MediaClient{config: mpoq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mpoq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mpoq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(modproductoption.Table, modproductoption.FieldID, selector),
			sqlgraph.To(media.Table, media.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, modproductoption.MediaTable, modproductoption.MediaColumn),
		)
		fromU = sqlgraph.SetNeighbors(mpoq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ModProductOption entity from the query.
// Returns a *NotFoundError when no ModProductOption was found.
func (mpoq *ModProductOptionQuery) First(ctx context.Context) (*ModProductOption, error) {
	nodes, err := mpoq.Limit(1).All(setContextOp(ctx, mpoq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{modproductoption.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) FirstX(ctx context.Context) *ModProductOption {
	node, err := mpoq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ModProductOption ID from the query.
// Returns a *NotFoundError when no ModProductOption ID was found.
func (mpoq *ModProductOptionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mpoq.Limit(1).IDs(setContextOp(ctx, mpoq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{modproductoption.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := mpoq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ModProductOption entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ModProductOption entity is found.
// Returns a *NotFoundError when no ModProductOption entities are found.
func (mpoq *ModProductOptionQuery) Only(ctx context.Context) (*ModProductOption, error) {
	nodes, err := mpoq.Limit(2).All(setContextOp(ctx, mpoq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{modproductoption.Label}
	default:
		return nil, &NotSingularError{modproductoption.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) OnlyX(ctx context.Context) *ModProductOption {
	node, err := mpoq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ModProductOption ID in the query.
// Returns a *NotSingularError when more than one ModProductOption ID is found.
// Returns a *NotFoundError when no entities are found.
func (mpoq *ModProductOptionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mpoq.Limit(2).IDs(setContextOp(ctx, mpoq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{modproductoption.Label}
	default:
		err = &NotSingularError{modproductoption.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := mpoq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ModProductOptions.
func (mpoq *ModProductOptionQuery) All(ctx context.Context) ([]*ModProductOption, error) {
	ctx = setContextOp(ctx, mpoq.ctx, ent.OpQueryAll)
	if err := mpoq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ModProductOption, *ModProductOptionQuery]()
	return withInterceptors[[]*ModProductOption](ctx, mpoq, qr, mpoq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) AllX(ctx context.Context) []*ModProductOption {
	nodes, err := mpoq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ModProductOption IDs.
func (mpoq *ModProductOptionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if mpoq.ctx.Unique == nil && mpoq.path != nil {
		mpoq.Unique(true)
	}
	ctx = setContextOp(ctx, mpoq.ctx, ent.OpQueryIDs)
	if err = mpoq.Select(modproductoption.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := mpoq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mpoq *ModProductOptionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mpoq.ctx, ent.OpQueryCount)
	if err := mpoq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mpoq, querierCount[*ModProductOptionQuery](), mpoq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) CountX(ctx context.Context) int {
	count, err := mpoq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mpoq *ModProductOptionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mpoq.ctx, ent.OpQueryExist)
	switch _, err := mpoq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mpoq *ModProductOptionQuery) ExistX(ctx context.Context) bool {
	exist, err := mpoq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ModProductOptionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mpoq *ModProductOptionQuery) Clone() *ModProductOptionQuery {
	if mpoq == nil {
		return nil
	}
	return &ModProductOptionQuery{
		config:     mpoq.config,
		ctx:        mpoq.ctx.Clone(),
		order:      append([]modproductoption.OrderOption{}, mpoq.order...),
		inters:     append([]Interceptor{}, mpoq.inters...),
		predicates: append([]predicate.ModProductOption{}, mpoq.predicates...),
		withMod:    mpoq.withMod.Clone(),
		withMedia:  mpoq.withMedia.Clone(),
		// clone intermediate query.
		sql:  mpoq.sql.Clone(),
		path: mpoq.path,
	}
}

// WithMod tells the query-builder to eager-load the nodes that are connected to
// the "mod" edge. The optional arguments are used to configure the query builder of the edge.
func (mpoq *ModProductOptionQuery) WithMod(opts ...func(*ModQuery)) *ModProductOptionQuery {
	query := (&ModClient{config: mpoq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mpoq.withMod = query
	return mpoq
}

// WithMedia tells the query-builder to eager-load the nodes that are connected to
// the "media" edge. The optional arguments are used to configure the query builder of the edge.
func (mpoq *ModProductOptionQuery) WithMedia(opts ...func(*MediaQuery)) *ModProductOptionQuery {
	query := (&MediaClient{config: mpoq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mpoq.withMedia = query
	return mpoq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ModProductOption.Query().
//		GroupBy(modproductoption.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mpoq *ModProductOptionQuery) GroupBy(field string, fields ...string) *ModProductOptionGroupBy {
	mpoq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ModProductOptionGroupBy{build: mpoq}
	grbuild.flds = &mpoq.ctx.Fields
	grbuild.label = modproductoption.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.ModProductOption.Query().
//		Select(modproductoption.FieldCreateTime).
//		Scan(ctx, &v)
func (mpoq *ModProductOptionQuery) Select(fields ...string) *ModProductOptionSelect {
	mpoq.ctx.Fields = append(mpoq.ctx.Fields, fields...)
	sbuild := &ModProductOptionSelect{ModProductOptionQuery: mpoq}
	sbuild.label = modproductoption.Label
	sbuild.flds, sbuild.scan = &mpoq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ModProductOptionSelect configured with the given aggregations.
func (mpoq *ModProductOptionQuery) Aggregate(fns ...AggregateFunc) *ModProductOptionSelect {
	return mpoq.Select().Aggregate(fns...)
}

func (mpoq *ModProductOptionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mpoq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mpoq); err != nil {
				return err
			}
		}
	}
	for _, f := range mpoq.ctx.Fields {
		if !modproductoption.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mpoq.path != nil {
		prev, err := mpoq.path(ctx)
		if err != nil {
			return err
		}
		mpoq.sql = prev
	}
	return nil
}

func (mpoq *ModProductOptionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ModProductOption, error) {
	var (
		nodes       = []*ModProductOption{}
		withFKs     = mpoq.withFKs
		_spec       = mpoq.querySpec()
		loadedTypes = [2]bool{
			mpoq.withMod != nil,
			mpoq.withMedia != nil,
		}
	)
	if mpoq.withMod != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, modproductoption.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ModProductOption).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ModProductOption{config: mpoq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mpoq.modifiers) > 0 {
		_spec.Modifiers = mpoq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mpoq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mpoq.withMod; query != nil {
		if err := mpoq.loadMod(ctx, query, nodes, nil,
			func(n *ModProductOption, e *Mod) { n.Edges.Mod = e }); err != nil {
			return nil, err
		}
	}
	if query := mpoq.withMedia; query != nil {
		if err := mpoq.loadMedia(ctx, query, nodes,
			func(n *ModProductOption) { n.Edges.Media = []*Media{} },
			func(n *ModProductOption, e *Media) { n.Edges.Media = append(n.Edges.Media, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range mpoq.withNamedMedia {
		if err := mpoq.loadMedia(ctx, query, nodes,
			func(n *ModProductOption) { n.appendNamedMedia(name) },
			func(n *ModProductOption, e *Media) { n.appendNamedMedia(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range mpoq.loadTotal {
		if err := mpoq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mpoq *ModProductOptionQuery) loadMod(ctx context.Context, query *ModQuery, nodes []*ModProductOption, init func(*ModProductOption), assign func(*ModProductOption, *Mod)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ModProductOption)
	for i := range nodes {
		if nodes[i].mod_product_options == nil {
			continue
		}
		fk := *nodes[i].mod_product_options
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(mod.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "mod_product_options" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (mpoq *ModProductOptionQuery) loadMedia(ctx context.Context, query *MediaQuery, nodes []*ModProductOption, init func(*ModProductOption), assign func(*ModProductOption, *Media)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ModProductOption)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Media(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(modproductoption.MediaColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.mod_product_option_media
		if fk == nil {
			return fmt.Errorf(`foreign-key "mod_product_option_media" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "mod_product_option_media" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (mpoq *ModProductOptionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mpoq.querySpec()
	if len(mpoq.modifiers) > 0 {
		_spec.Modifiers = mpoq.modifiers
	}
	_spec.Node.Columns = mpoq.ctx.Fields
	if len(mpoq.ctx.Fields) > 0 {
		_spec.Unique = mpoq.ctx.Unique != nil && *mpoq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mpoq.driver, _spec)
}

func (mpoq *ModProductOptionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(modproductoption.Table, modproductoption.Columns, sqlgraph.NewFieldSpec(modproductoption.FieldID, field.TypeUUID))
	_spec.From = mpoq.sql
	if unique := mpoq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mpoq.path != nil {
		_spec.Unique = true
	}
	if fields := mpoq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, modproductoption.FieldID)
		for i := range fields {
			if fields[i] != modproductoption.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mpoq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mpoq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mpoq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mpoq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mpoq *ModProductOptionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mpoq.driver.Dialect())
	t1 := builder.Table(modproductoption.Table)
	columns := mpoq.ctx.Fields
	if len(columns) == 0 {
		columns = modproductoption.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mpoq.sql != nil {
		selector = mpoq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mpoq.ctx.Unique != nil && *mpoq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mpoq.predicates {
		p(selector)
	}
	for _, p := range mpoq.order {
		p(selector)
	}
	if offset := mpoq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mpoq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedMedia tells the query-builder to eager-load the nodes that are connected to the "media"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (mpoq *ModProductOptionQuery) WithNamedMedia(name string, opts ...func(*MediaQuery)) *ModProductOptionQuery {
	query := (&MediaClient{config: mpoq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if mpoq.withNamedMedia == nil {
		mpoq.withNamedMedia = make(map[string]*MediaQuery)
	}
	mpoq.withNamedMedia[name] = query
	return mpoq
}

// ModProductOptionGroupBy is the group-by builder for ModProductOption entities.
type ModProductOptionGroupBy struct {
	selector
	build *ModProductOptionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mpogb *ModProductOptionGroupBy) Aggregate(fns ...AggregateFunc) *ModProductOptionGroupBy {
	mpogb.fns = append(mpogb.fns, fns...)
	return mpogb
}

// Scan applies the selector query and scans the result into the given value.
func (mpogb *ModProductOptionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mpogb.build.ctx, ent.OpQueryGroupBy)
	if err := mpogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ModProductOptionQuery, *ModProductOptionGroupBy](ctx, mpogb.build, mpogb, mpogb.build.inters, v)
}

func (mpogb *ModProductOptionGroupBy) sqlScan(ctx context.Context, root *ModProductOptionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mpogb.fns))
	for _, fn := range mpogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mpogb.flds)+len(mpogb.fns))
		for _, f := range *mpogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mpogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mpogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ModProductOptionSelect is the builder for selecting fields of ModProductOption entities.
type ModProductOptionSelect struct {
	*ModProductOptionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mpos *ModProductOptionSelect) Aggregate(fns ...AggregateFunc) *ModProductOptionSelect {
	mpos.fns = append(mpos.fns, fns...)
	return mpos
}

// Scan applies the selector query and scans the result into the given value.
func (mpos *ModProductOptionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mpos.ctx, ent.OpQuerySelect)
	if err := mpos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ModProductOptionQuery, *ModProductOptionSelect](ctx, mpos.ModProductOptionQuery, mpos, mpos.inters, v)
}

func (mpos *ModProductOptionSelect) sqlScan(ctx context.Context, root *ModProductOptionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mpos.fns))
	for _, fn := range mpos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mpos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mpos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
